import { Node } from '@tiptap/pm/model';
import { Position } from '../../types';
export declare const textBetween: (from: number, to: number, doc: Node) => Position[];
export default textBetween;
